[project]
name = "analytics"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "django",
    "dnspython",
    "gunicorn",
    "requests",
    "tzdata",
    "user-agents",
    "uvicorn",
    "whitenoise",
    "geoip2",
]
