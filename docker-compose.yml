services:
  web:
    container_name: analytics
    build: .
    init: true
    env_file: .env
    environment:
      ANALYTICS_DATA_DIR: /data
    volumes:
      - /srv/data/analytics:/data
    restart: unless-stopped
